/** Glaze Atlas tab — route file name is legacy; module id is `glaze-atlas`. */
import JournalScreen from '@/src/screens/library/JournalScreen';
import React from 'react';

export default function GlazeAtlasTab() {
  return <JournalScreen />;
}