/** Scroll content padding above the tab bar — tight clearance without excess empty scroll. */
export const TAB_SCROLL_BOTTOM_PADDING = 56;

/** Distance from the tab content bottom for floating action buttons. */
export const TAB_FLOATING_ACTION_BOTTOM = 16;

/** Extra scroll padding when a ~52px FAB sits above the tab bar. */
export const TAB_SCROLL_BOTTOM_PADDING_WITH_FAB =
  TAB_FLOATING_ACTION_BOTTOM + 52 + 8;
